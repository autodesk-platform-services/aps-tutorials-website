Start the application from Visual Studio (for example, via the _Debug > Start Debugging_
menu, or by pressing `F5`), and it will automatically open [http://localhost:8080](http://localhost:8080)
in your browser. You should get a 404 response because we haven't implemented any server logic yet.
That's going to be the goal of the next step.
